import { QuestionForCategoryPipe } from './question-for-category.pipe';

describe('QuestionForCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionForCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
